bin/
/sdd-hello-world
.claude/

# Local credentials — never committed. See GH history: a Claude OAuth token
# was tracked here and public from 2026-03-01; the tokens were revoked and the
# file untracked. Nothing under .secrets/ belongs in git.
.secrets/
